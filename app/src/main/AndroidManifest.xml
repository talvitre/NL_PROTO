<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nlprototype">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".NewSheet"
            android:label="@string/title_activity_new_sheet"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".NotePanel"
            android:label="@string/title_activity_note_panel"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".InstrumentPanel"
            android:label="InstrumentPanel"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MusicSheet"
            android:label="@string/title_activity_music_sheet"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ProjectsPage"
            android:label="@string/title_activity_projects_page"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>